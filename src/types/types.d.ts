import { BigNumber } from "bignumber.js"; // or from 'ethers' if you're using ethers.js

export interface CampaignDetails {
  0: BigNumber; // or number, depending on the library you're using
  1: BigNumber;
  2: BigNumber;
  3: BigNumber;
  4: string;
}
